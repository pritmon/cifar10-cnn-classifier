# Dependencies for the Hugging Face Space.
# Gradio itself is provided by the Space SDK (see sdk_version in README.md),
# so we only list what the model needs here.
tensorflow-cpu
pillow
numpy
# Python 3.13 removed the built-in 'audioop' module that some Gradio audio
# helpers import. This backport restores it (only installed on Python 3.13+).
audioop-lts; python_version >= "3.13"
